(ns extracli.ksh
    (:require [extracli.kb :as kb]) )


;;; générateur de tâches

(comment "chaque lundi avant 12h, mettre à jour le projet extraction-client sous PS-Next"
  {:comprehension   "oui, cad ?"
   :prise-en-compte "non, je ne vois pas comment" })

(comment "chaque vendredi avant 12h, mettre à jour mes activités IMA sous PS-next")

(comment "traiter tous les mails qui me sont destinés")

